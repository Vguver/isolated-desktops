# Examples

This directory contains small sample files you can copy and adapt.

Files:

- `profile-override.example.json` - example custom manifest override
- `jakoolit-preset.example.env` - example environment file or notes you can adapt when testing preset-like flows

These files are examples only. They are not loaded automatically.
