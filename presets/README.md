# Presets

A preset is a plain text file with one profile name per line.

Use it with:

```bash
idtool preset list
idtool preset show hyprland-suite
idtool preset install hyprland-suite
```

Presets are useful when you repeatedly test the same group of profiles.
